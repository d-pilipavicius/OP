package swing_changes;

import javax.swing.*;

public class ImprovedCheckBox extends JCheckBox {
    public ImprovedCheckBox(int x, int y, int width, int height){
        super();
        setSize(width, height);
        setLocation(x, y);
    }
}
